type Query {
  bookById(id: ID): Book
  allBooks: [Book!]!
  orderById(id: ID): Order
}

type Mutation {
  createOrder(bookId: ID): Order
}

type Book {
  id: ID
  name: String
  pageCount: Int
  author: Author
}

type Author {
  id: ID
  firstName: String
  lastName: String
}

type Order {
  id: ID
  book: Book
}
